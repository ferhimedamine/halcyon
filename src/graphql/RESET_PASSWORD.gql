mutation ResetPassword(
    $token: String!
    $emailAddress: String!
    $newPassword: String!
) {
    resetPassword(
        token: $token
        emailAddress: $emailAddress
        newPassword: $newPassword
    ) {
        message
    }
}
